# SQL_AACT_DATABASE
SQL portfolio project analyzing oncology clinical trials using the AACT (ClinicalTrials.gov) database. Covers joins, CTEs, window functions (RANK, LAG, LEAD), views, and data quality investigation across 600K+ trial records in PostgreSQL.
